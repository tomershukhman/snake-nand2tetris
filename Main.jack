class Main {

    /** Initializes a new game and starts it. */    
    function void main() {
        var SnakeGame game;
        let game = SnakeGame.new();
        do game.run();
	    //do game.dispose();
        return;
    }
}